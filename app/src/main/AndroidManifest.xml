<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.CadapioAndroid">

        <activity
            android:name="com.goomer.ps.legacy.MenuDetailActivity"
            android:exported="false"
            android:parentActivityName="com.goomer.ps.presentation.MenuListActivity" />

        <activity
            android:name="com.goomer.ps.presentation.MenuListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
